import 'package:flutter/material.dart';

class BlocklyWorkspaceScreen extends StatelessWidget {
  const BlocklyWorkspaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Blockly Workspace Screen'),
      ),
    );
  }
}
